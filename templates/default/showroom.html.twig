<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
</head>
<body>
<div class="container">
    <div class="row">
        {% for article in  articles  %}
            <div class="container" style = "margin-bottom: 25px">
                <p style="font-weight: bold; text-decoration-style: solid"> {{ article.title |capitalize }} </p>
                <p> {{ article.content }} </p>
                <p style="font-weight: lighter; font-style: italic"> {{ article.author }}</p>
                <button><a href="{{ path('formulaire2', {'id': article.id}) }}"> Modifier </a> </button>
                <button><a href="{{ path('formulaire3', {'id': article.id}) }}"> Supprimer </a> </button>


            </div>
        {% endfor %}
    </div>
</div>
</body>
</html>